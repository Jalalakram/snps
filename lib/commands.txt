flutter pub run build_runner watch --delete-conflicting-outputs

flutter create -t skeleton new_flutter_template

// @dart=2.11
flutter run --no-sound-null-safety

dart migrate --ignore-errors
dart migrate --skip-import-check

colorScheme.secondary   // --replace with following--
textTheme.headline1.copyWith(color: themeData.colorScheme.onSecondary,);

map(f), which takes each value of stream A, applies f() on it,
and produces a value on stream B.